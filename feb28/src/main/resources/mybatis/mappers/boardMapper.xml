<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="board">
	
	<select id="boardList" resultType="boardDTO">
		SELECT board_no, board_title, board_date, board_like, user_no, user_name
		FROM viewboard
		LIMIT 0, 10
	</select>
	
	<insert id="write" parameterType="boardDTO">
		INSERT INTO board (board_title, board_content, user_no)
		VALUES( #{board_title}, #{board_content}, #{user_no} ) 
	</insert>
		
	<insert id="write2" parameterType="HashMap">
		INSERT INTO board (board_title, board_content, user_no)
		VALUES( #{board_title}, #{board_content}, #{user_no} )
	</insert>
	
	<insert id="write1" parameterType="boardDTO">
		INSERT INTO board (board_title, board_content, user_no)
		VALUES( #{board_title}, #{board_content}, (SELECT user_no FROM user WHERE user_id=#{user_id}) ) 
	</insert>
	
	<select id="detail" resultType="boardDTO" parameterType="Integer">
		SELECT b.*, u.user_name, u.user_id
		FROM board b JOIN user u ON b.user_no=u.user_no
		WHERE board_no=#{board_no} AND board_del=1
	</select>
	
	<update id="del" parameterType="boardDTO">
		UPDATE board SET board_del=0
		WHERE board_no=#{board_no}
		AND user_no=(SELECT board_no FROM user WHERE user_id=#{user_id} )
	</update>
	
	<select id="update" parameterType="boardDTO" resultType="boardDTO">
      SELECT b.*, u.user_name, u.user_id
      FROM board b JOIN user u ON b.user_no=u.user_no
      WHERE b.board_no=#{board_no} 
      AND b.board_del=1
      AND b.user_no=(SELECT user_no FROM user WHERE user_id=#{user_id})      
   </select>
	
		
</mapper>